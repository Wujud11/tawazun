# Mission ID
M03 — Code Cleanup and Reusable Components

# Reason for Rollback
M03 modifies three page files and creates one new component file. A rollback is
needed if:
- Any of the three pages throw a runtime error or render incorrectly after the refactor.
- The TypeScript check fails with errors that cannot be resolved quickly in the
  allowed files.
- The build fails for any reason that cannot be isolated to the four allowed files.
- The `StatCardGrid` abstraction introduced a regression in layout, styling, or
  accessibility on any page.

# Files to Restore
- `src/pages/debts.tsx` — restore to its pre-M03 state (local KpiCards function)
- `src/pages/netting.tsx` — restore to its pre-M03 state (inline KPI grid)
- `src/pages/companies.tsx` — restore to its pre-M03 state (local CompanySummaryCards function)
- `src/components/ui/stat-cards.tsx` — delete (this file did not exist before M03)

No other files were changed by M03, so no other files need to be restored.

# Git Commands

Option A — Restore modified page files and delete new component (if M03 has not been committed):
```
git checkout HEAD -- src/pages/debts.tsx
git checkout HEAD -- src/pages/netting.tsx
git checkout HEAD -- src/pages/companies.tsx
git rm src/components/ui/stat-cards.tsx
```

Option B — If M03 was committed, identify the commit and revert it:
```
git log --oneline -5
git revert <commit-hash>
```

Option C — Inspect changes before reverting:
```
git diff HEAD src/pages/debts.tsx
git diff HEAD src/pages/netting.tsx
git diff HEAD src/pages/companies.tsx
git diff HEAD src/components/ui/stat-cards.tsx
```

# Verification
1. Confirm `src/components/ui/stat-cards.tsx` no longer exists.
2. Open `src/pages/debts.tsx` — confirm local `KpiCards` function and `kpiCards` array are present.
3. Open `src/pages/netting.tsx` — confirm inline KPI grid block is present in `NettingPage`.
4. Open `src/pages/companies.tsx` — confirm local `CompanySummaryCards` function and `summaryCards` array are present.
5. Run:
   ```
   npm run build
   ```
   Confirm the build passes (pre-M03 state was a clean build).
6. Run:
   ```
   npx tsc --noEmit
   ```
   Confirm 0 errors.
7. Open all four pages in the browser and confirm they render correctly.

# Rollback Complete
[ ] Rollback applied
[ ] stat-cards.tsx deleted
[ ] debts.tsx restored
[ ] netting.tsx restored
[ ] companies.tsx restored
[ ] Build verified passing
[ ] TypeScript verified clean
[ ] All four pages verified in browser
[ ] Ready to retry or escalate

Notes:
