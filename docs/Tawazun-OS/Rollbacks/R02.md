# Mission ID
M02 — Mock Data Consistency and Cleanup

# Reason for Rollback
M02 modifies only `src/data/dashboard-mock.ts`. A rollback is needed if:
- The added companies caused a TypeScript error or build failure that cannot be fixed quickly.
- The UI broke in a way that cannot be isolated to dashboard-mock.ts.
- The wrong data was removed or the wrong exports were deleted.
- The Companies page or Dashboard now shows incorrect totals or layout errors.

# Files to Restore
- `src/data/dashboard-mock.ts` — restore to its pre-M02 state (6 companies, with the two dead format exports still present)

No other files were changed by M02, so no other files need to be restored.

# Git Commands
Option A — Restore dashboard-mock.ts to its last committed state (if M02 has not been committed):
```
git checkout HEAD -- src/data/dashboard-mock.ts
```

Option B — If M02 was committed, identify the commit and revert it:
```
git log --oneline -5
git revert <commit-hash>
```

Option C — If you need to inspect what changed before reverting:
```
git diff HEAD src/data/dashboard-mock.ts
```

# Verification
1. Run `git status` — confirm `src/data/dashboard-mock.ts` is back to its pre-M02 state.
2. Open `src/data/dashboard-mock.ts` and confirm the `companies` array has 6 entries.
3. Confirm `formatSar` and `formatCompactSar` exports are present again (they were the pre-M02 state).
4. Run:
   ```
   npm run build
   ```
   Confirm the build passes (it should — the pre-M02 state was a clean build).
5. Run:
   ```
   npx tsc --noEmit
   ```
   Confirm 0 errors.
6. Open all four pages in the browser and confirm they load correctly.

# Rollback Complete
[ ] Rollback applied
[ ] dashboard-mock.ts restored to pre-M02 state
[ ] Build verified passing
[ ] TypeScript verified clean
[ ] All four pages verified in browser
[ ] Ready to retry or escalate

Notes:
