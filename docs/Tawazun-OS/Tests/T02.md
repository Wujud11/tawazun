# Mission ID
M02 — Mock Data Consistency and Cleanup

# Purpose
Verify that the two missing companies were added to dashboard-mock.ts, that the dead format exports were removed, that no other data was changed, and that the build and TypeScript checks still pass.

# Manual Tests
1. Open `src/data/dashboard-mock.ts` and confirm the `companies` array contains exactly 8 entries.
2. Confirm شركة زين للتطوير is present in the `companies` array with a valid `id`, `name`, `nameEn`, `sector`, `netBalance`, `totalPayable`, `totalReceivable`, and `activeDebts`.
3. Confirm شركة حاء للاستشارات is present in the `companies` array with the same required fields.
4. Confirm `formatSar` is no longer exported from `dashboard-mock.ts`.
5. Confirm `formatCompactSar` is no longer exported from `dashboard-mock.ts`.
6. Open `src/lib/format.ts` and confirm it is unchanged — `formatSar`, `formatPercent`, and `formatRelativeTime` still present.
7. Run `git diff src/data/debts-mock.ts` — must show no changes.
8. Run `git diff src/data/netting-mock.ts` — must show no changes.
9. Run `git status` — the only modified file must be `src/data/dashboard-mock.ts`.

# Build Verification
1. Run:
   ```
   npm run build
   ```
2. Confirm exit code 0 — build must pass with no new errors.
3. Confirm the 891 kB bundle warning is the only warning (pre-existing, non-blocking).

# UI Verification
1. Start the dev server:
   ```
   npm run dev
   ```
2. Open `/` — Dashboard: confirm KPI cards, charts, and tables render without errors.
3. Open `/companies` — Companies page: confirm the table now shows 8 companies including زين and حاء.
4. Open `/debts` — Debts page: confirm all 14 debt records still render. Confirm زين and حاء appear as creditor/debtor names without visual issues.
5. Open `/netting` — Netting page: confirm it loads and the "تشغيل المقاصة" button works.
6. Open browser console on each page — confirm no runtime errors.

# Logic Verification
1. For شركة زين للتطوير, verify the mock values are mathematically consistent with its records in debts-mock.ts:
   - dt-06: زين is creditor → receivable contribution: 175,000
   - dt-09: زين is debtor → payable contribution: 1,200,000
   - dt-14: زين is creditor → receivable contribution: 88,000
   - Expected totalReceivable = 263,000
   - Expected totalPayable = 1,200,000
   - Expected netBalance = −937,000
   - Expected activeDebts = 3
2. For شركة حاء للاستشارات, verify consistency with its active records:
   - dt-07: حاء is creditor → receivable: 490,000 (pending — active)
   - dt-13: حاء is debtor → payable: 540,000 (settled — excluded)
   - Expected totalReceivable = 490,000
   - Expected totalPayable = 0
   - Expected netBalance = 490,000
   - Expected activeDebts = 1
3. Run:
   ```
   npx tsc --noEmit
   ```
   Confirm 0 errors.

# Pass Criteria
The mission passes only if ALL of the following are true:
- `companies` array in `dashboard-mock.ts` contains exactly 8 entries
- شركة زين للتطوير is present with all required Company type fields
- شركة حاء للاستشارات is present with all required Company type fields
- `formatSar` is no longer exported from `dashboard-mock.ts`
- `formatCompactSar` is no longer exported from `dashboard-mock.ts`
- `debts-mock.ts` is unchanged
- `netting-mock.ts` is unchanged
- `lib/format.ts` is unchanged
- `npm run build` exits with code 0
- `npx tsc --noEmit` exits with 0 errors
- All four pages load without runtime errors in the browser

# Result
[x] Pass
[ ] Fail
[ ] Partial

Notes:
- Build: PASS (exit 0, 879 modules, pre-existing 500 kB warning only)
- TypeScript: PASS (0 errors, strict mode)
- Browser verification: pending manual check per UI Verification steps above
