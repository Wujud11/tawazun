# Mission ID
M03 — Code Cleanup and Reusable Components

# Purpose
Verify that the duplicated KPI card grid pattern has been extracted into a single
shared component, that all three pages use it, that the dashboard component is
unchanged, and that the build and TypeScript checks pass.

# Static Code Verification
1. Confirm `src/components/ui/stat-cards.tsx` exists.
2. Open `stat-cards.tsx` — confirm it exports `StatCardGrid` and defines `StatCard` type.
3. Open `src/pages/debts.tsx`:
   - Confirm it imports `StatCardGrid` from `@/components/ui/stat-cards`.
   - Confirm there is no local `function KpiCards` definition.
   - Confirm there is no standalone `kpiCards` array definition that contains Card JSX logic.
4. Open `src/pages/netting.tsx`:
   - Confirm it imports `StatCardGrid` from `@/components/ui/stat-cards`.
   - Confirm there is no inline `<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">` KPI block inside `NettingPage`.
5. Open `src/pages/companies.tsx`:
   - Confirm it imports `StatCardGrid` from `@/components/ui/stat-cards`.
   - Confirm there is no local `function CompanySummaryCards` definition.
   - Confirm there is no local `summaryCards` array using `suffix` key (should be `sub`).
6. Open `src/components/dashboard/kpi-cards.tsx` — confirm it is unchanged from its
   pre-M03 state (still imports `dashboardKpis`, still has change-percentage indicator).
7. Run `git diff src/components/dashboard/kpi-cards.tsx` — must show no changes.
8. Run `git status` — modified files must be exactly:
   - `src/components/ui/stat-cards.tsx` (new)
   - `src/pages/debts.tsx`
   - `src/pages/netting.tsx`
   - `src/pages/companies.tsx`

# Build Verification
1. Run:
   ```
   npm run build
   ```
2. Confirm exit code 0 — no new errors.
3. Confirm the pre-existing 500 kB chunk warning is the only warning.

# TypeScript Verification
1. Run:
   ```
   npx tsc --noEmit
   ```
2. Confirm exit code 0 — 0 errors.

# UI Verification
1. Start the dev server:
   ```
   npm run dev
   ```
2. Open `/debts` — confirm the 4 KPI cards render identically to pre-M03:
   - إجمالي قيمة الديون
   - عدد سجلات الدين
   - متوسط الدين
   - أكبر دين
3. Open `/netting` — confirm the 4 KPI cards render identically to pre-M03:
   - التحويلات قبل المقاصة
   - التحويلات بعد المقاصة
   - نسبة التخفيض
   - إجمالي قيمة الديون
4. Open `/companies` — confirm the 4 KPI cards render identically to pre-M03:
   - عدد الشركات
   - إجمالي المستحق الدفع
   - إجمالي المستحق القبض
   - الصافي الإجمالي للشبكة
5. Open `/` (Dashboard) — confirm dashboard KPI cards still render correctly
   (these use the separate shared `KpiCards` component, not `StatCardGrid`).
6. Open browser console on all four pages — confirm no runtime errors.

# Pass Criteria
The mission passes only if ALL of the following are true:
- `src/components/ui/stat-cards.tsx` exists and exports `StatCardGrid`
- `src/pages/debts.tsx` uses `StatCardGrid` — no local KpiCards function
- `src/pages/netting.tsx` uses `StatCardGrid` — no inline KPI grid block
- `src/pages/companies.tsx` uses `StatCardGrid` — no local CompanySummaryCards function
- `src/components/dashboard/kpi-cards.tsx` is unchanged
- No other application files were modified
- `npm run build` exits with code 0
- `npx tsc --noEmit` exits with 0 errors
- All four pages render correctly in the browser with no console errors

# Result
[x] Pass
[ ] Fail
[ ] Partial

Notes:
- Build: PASS (exit 0, 880 modules, pre-existing 500 kB warning only)
- TypeScript: PASS (0 errors, strict mode)
- Browser verification: pending manual check per UI Verification steps above
