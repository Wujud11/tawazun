# Mission ID
M02 � Mock Data Consistency and Cleanup

# Objective
Make all current mock data internally consistent without changing the product scope or UI behavior. Every company referenced anywhere in the mock data must exist in the canonical company list. Dead duplicate exports must be removed.

# Why this mission exists
The M01 audit identified two concrete data inconsistencies:
1. `debts-mock.ts` references two companies � ???? ??? ??????? and ???? ??? ?????????? � that do not appear in the `companies` array in `dashboard-mock.ts`. This means the Debts page shows creditors and debtors that are invisible on the Companies page and Dashboard.
2. `dashboard-mock.ts` exports `formatSar` and `formatCompactSar` functions that are confirmed dead (never imported by any consumer file). These duplicate the canonical implementations in `lib/format.ts`.

These issues must be resolved before any feature development begins. Allowing inconsistent data into M03+ creates compounding errors that are harder to fix later.

# Inputs
- `src/data/dashboard-mock.ts`
- `src/data/debts-mock.ts`
- `src/data/netting-mock.ts`
- `src/types/dashboard.ts`
- `src/lib/format.ts`
- `src/pages/debts.tsx`
- `src/pages/companies.tsx`
- `src/pages/dashboard.tsx`
- `src/components/dashboard/debt-summary.tsx`
- `src/components/dashboard/companies-summary.tsx`
- `docs/Tawazun-OS/Missions/M01.md` (audit findings)

# Outputs
- `dashboard-mock.ts`: dead `formatSar` and `formatCompactSar` exports removed; two new companies added (???? ??? ??????? and ???? ??? ??????????) to the `companies` array with realistic mock data that is consistent with their debt records.
- `debts-mock.ts`: unchanged.
- No other files changed.
- `npm run build` passes.
- `npx tsc --noEmit` passes.

# Dependencies
M01 � Project Audit (completed)

# Files that may be modified
- `src/data/dashboard-mock.ts`

# Files that must never be modified
- `src/data/debts-mock.ts`
- `src/data/netting-mock.ts`
- `src/lib/format.ts`
- `src/lib/utils.ts`
- `src/lib/motion.ts`
- Any file inside `src/components/`
- Any file inside `src/pages/`
- Any file inside `src/types/`
- Any file inside `src/providers/`
- Any file inside `src/routes/`
- Any file inside `src/hooks/`
- `src/main.tsx`
- `src/App.tsx`
- `src/styles/globals.css`
- `package.json`
- `package-lock.json`
- `vite.config.ts`
- `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`
- `index.html`
- `components.json`
- Any documentation file

# Acceptance Criteria
- Every company referenced in `debts-mock.ts` exists in the `companies` array in `dashboard-mock.ts`.
- The dead `formatSar` and `formatCompactSar` exports are removed from `dashboard-mock.ts`.
- `npm run build` passes with no new errors.
- `npx tsc --noEmit` passes with no errors.
- All four pages load and render correctly in the browser.
- No UI layout, behavior, or visible content is broken.

# Completion Checklist
- [x] `debts-mock.ts` read and all referenced companies identified
- [x] `dashboard-mock.ts` read and existing companies listed
- [x] Missing companies identified: ???? ??? ??????? and ???? ??? ??????????
- [x] Both missing companies added to `companies` array in `dashboard-mock.ts` with consistent mock data
- [x] Dead `formatSar` export removed from `dashboard-mock.ts`
- [x] Dead `formatCompactSar` export removed from `dashboard-mock.ts`
- [x] No other changes made to `dashboard-mock.ts`
- [x] `npm run build` run and confirmed passing
- [x] `npx tsc --noEmit` run and confirmed passing
- [ ] All four pages verified in browser

# Linked Prompt
Prompts/P02.txt

# Linked Test
Tests/T02.md

# Linked Rollback
Rollbacks/R02.md

---

## Execution Report

**Date:** 2026-07-07
**Executor:** Cursor Agent (M02 execution)

### Data Decisions

**???? ??? ???????** (id: `c7`, nameEn: `Zain Development`, sector: `?????`)
Derived from active (non-settled) debt records in `debts-mock.ts`:
- dt-06: ??? is creditor ? receivable: 175,000 (overdue)
- dt-09: ??? is debtor ? payable: 1,200,000 (pending)
- dt-14: ??? is creditor ? receivable: 88,000 (pending)
- totalReceivable: 263,000
- totalPayable: 1,200,000
- netBalance: ?937,000
- activeDebts: 3

**???? ??? ??????????** (id: `c8`, nameEn: `Haa Consulting`, sector: `????????`)
Derived from active (non-settled) debt records only:
- dt-07: ??? is creditor ? receivable: 490,000 (pending � active)
- dt-13: ??? is debtor ? payable: 540,000 (settled � excluded from active count)
- totalReceivable: 490,000
- totalPayable: 0 (no active payable records)
- netBalance: 490,000
- activeDebts: 1

### Files Changed
- `src/data/dashboard-mock.ts` � added 2 companies (c7, c8), removed `formatSar` and `formatCompactSar` exports

### Build Result
**PASS** � `npm run build` exited with code 0.
- 879 modules transformed (unchanged from M01 baseline)
- `dist/assets/index-D_2aFQsc.js` � 891.82 kB (gzip: 269.51 kB)
- Only pre-existing warning: single chunk exceeds 500 kB advisory. Non-blocking.

### TypeScript Result
**PASS** � `npx tsc --noEmit` exited with code 0. Zero errors.

### Remaining Risks
- The `companyDebtShares` array in `dashboard-mock.ts` still contains only 6 entries (??? through ???). The bar chart on the Dashboard will not show ??? or ???. This is a pre-existing limitation of the chart mock data scope � it is not broken behaviour, and adding chart entries for the two new companies was not part of M02's scope.
- Browser UI verification (all four pages loading without runtime errors) must still be performed manually per T02.md. Automated checks pass.
- The `active-companies` KPI value is hardcoded as `'24'` � it does not reflect the actual count in the `companies` array and was not in scope for M02 to fix.
