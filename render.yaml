services:
  - type: web
    name: tawazun-api
    runtime: node
    buildCommand: npm install && npm run render:build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false
      - key: FRONTEND_URL
        sync: false
